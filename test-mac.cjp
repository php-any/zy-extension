<?cjp
// 测试 macOS 支持的 Origami 文件

函数 测试函数() {
    变量 $message = "Hello from macOS!";
    打印($message);
}

类 测试类 {
    属性 $name;
    
    构造函数($name) {
        这个->name = $name;
    }
    
    方法 获取名称() {
        返回 这个->name;
    }
}

// 主程序
$obj = 新 测试类("macOS测试");
测试函数();
打印($obj->获取名称());
?>