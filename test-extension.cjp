// 测试 Origami 语言扩展功能
// Test file for Origami language extension

// 基本语法测试
function testFunction() {
    var message = "Hello, Origami!";
    print(message);
}

// 变量声明测试
var number = 42;
var string = "test string";
var boolean = true;

// 控制流测试
if (number > 0) {
    print("Positive number");
} else {
    print("Non-positive number");
}

// 循环测试
for (var i = 0; i < 5; i++) {
    print("Loop iteration: " + i);
}

// 函数调用测试
testFunction();

// 注释测试
/* 这是一个
   多行注释 */

// 错误测试 - 故意的语法错误用于测试诊断
// var unclosedString = "this string is not closed